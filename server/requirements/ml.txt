# =============================================================================
# Midori — Machine Learning dependencies
# Install: pip install -r requirements/ml.txt
# (automatically installs base.txt via -r)
#
# Python version: 3.13.0  (installed)
# TensorFlow:     2.21.0  (latest release with Python 3.13 wheels)
#
# Note on Keras:
#   TF 2.16+ ships with Keras 3 by default.
#   The engine imports Keras via  tf.keras  which resolves to Keras 3.
#   If you need the legacy Keras 2 API set  TF_USE_LEGACY_KERAS=1
#   and run:  pip install tf_keras
# =============================================================================

-r base.txt

# ── TensorFlow (includes Keras 3) ─────────────────────────────────────────────
tensorflow==2.21.0
numpy>=1.26.4

# ── TF transitive dependencies (key ones pinned) ─────────────────────────────
absl-py>=1.4.0
astunparse>=1.6.3
flatbuffers>=23.5.26
gast>=0.4.0
google-auth>=2.22.0
google-auth-oauthlib>=1.0.0
google-pasta>=0.2.0
grpcio>=1.56.2
h5py>=3.9.0
opt-einsum>=3.3.0
packaging>=23.1
protobuf>=4.23.4
tensorboard>=2.13.0
termcolor>=2.3.0
typing-extensions>=4.7.1
wrapt>=1.15.0
