# =============================================================================
# Midori — Environment Variables  (.env)
# Production environment configuration
# =============================================================================

# ── Django ────────────────────────────────────────────────────────────────────
SECRET_KEY=Slyd)E^0oNTdZt0PkbSjIKbU6_T@Qb4OrsN%1KS^+M@taMS8r&
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,10.0.2.2,192.168.29.92

# ── Database (PostgreSQL) ─────────────────────────────────────────────────────
DB_NAME=plant_disease
DB_USER=postgres
DB_PASSWORD=2004
DB_HOST=localhost
DB_PORT=5432

# ── CORS — allow Flutter app (dev) ───────────────────────────────────────────
# Add your device/emulator IP if testing on a real phone
CORS_ALLOWED_ORIGINS=http://localhost:8000,http://10.0.2.2:8000,http://192.168.29.92:8000

# ── ML model directory ────────────────────────────────────────────────────────
# Defaults to server/ml_models/ — only set if you want a custom path
# ML_MODELS_DIR=/absolute/path/to/ml_models

# ── Media root ────────────────────────────────────────────────────────────────
# Where uploaded images and Grad-CAM PNGs are saved
# MEDIA_ROOT=/absolute/path/to/media
